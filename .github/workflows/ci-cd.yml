name: CI/CD

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

permissions:
  contents: read
  packages: write

jobs:
  pipeline:
    uses: ArchGenTf/Main/.github/workflows/frontend.yml@master
    with:
      service: "archgen-frontend"
      image: "acrarchgen.azurecr.io/frontend"
      trigger: ${{ github.event_name }}
    secrets: inherit
